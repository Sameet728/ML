# ============================================================
# AI Quant Research Platform — VPS Requirements (lightweight)
# Excludes PyTorch (not used), weasyprint (complex deps)
# Tested on Ubuntu 20.04 with 1GB RAM + 4GB swap
# ============================================================

# Core data
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# Market data
ccxt>=4.3.0
yfinance>=0.2.40

# Technical indicators (install from GitHub — PyPI version not Python 3.11 compatible)
# pandas-ta installed separately via: pip install git+https://github.com/twopirllc/pandas-ta

# Machine learning
scikit-learn>=1.5.0
xgboost>=2.0.3
optuna>=3.6.0
joblib>=1.4.0
shap>=0.45.0

# Optuna SQLite storage backend
sqlalchemy>=2.0.0

# Hidden Markov Models (regime detection)
hmmlearn>=0.3.2

# Visualization (no kaleido on VPS — saves memory)
plotly>=5.22.0
matplotlib>=3.9.0

# Reporting
jinja2>=3.1.4

# Utilities
python-dotenv>=1.0.1
tqdm>=4.66.4
loguru>=0.7.2
diskcache>=5.6.3
pyarrow>=16.1.0
fastparquet>=2024.5.0

# NOTE: torch, torchvision, vectorbt, streamlit, weasyprint excluded on VPS
# (ANN disabled, dashboard runs locally, HTML reports used instead)
